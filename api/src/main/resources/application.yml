spring:
  mvc:
    favicon:
      enabled: false
  config:
    import:
      - app.yml
      - mail.yml

  profiles:
    active: local

  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false

  datasource:
    url: jdbc:mysql://discord-server.cp84w6mkcfno.ap-northeast-2.rds.amazonaws.com/discord?serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: 1q2w3e4r1Q!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace